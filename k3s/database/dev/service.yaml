apiVersion: v1
kind: Service
metadata:
  name: database
  namespace: architecture-study-database-dev
  labels:
    app: database
    environment: dev

spec:
  type: ClusterIP

  selector:
    app: database

  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      protocol: TCP
