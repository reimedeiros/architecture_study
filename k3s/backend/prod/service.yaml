apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: architecture-study-backend-prod
  labels:
    app: backend
    environment: prod

spec:
  type: ClusterIP

  selector:
    app: backend

  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
