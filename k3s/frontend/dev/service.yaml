apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: architecture-study-frontend-dev
  labels:
    app: frontend
    environment: dev

spec:
  type: ClusterIP

  selector:
    app: frontend

  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
