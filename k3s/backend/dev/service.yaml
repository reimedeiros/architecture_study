apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: architecture-study-backend-dev
  labels:
    app: backend
    environment: dev

spec:
  type: ClusterIP

  selector:
    app: backend

  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
